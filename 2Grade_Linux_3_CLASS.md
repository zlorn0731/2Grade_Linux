# 👨‍💻리눅스와 유닉스

## 4장 문서 편집기 (vi editor)
- vi
  - 리눅스에서 가장 일반적인 화면 편집기, 기본 vi의 개정판인 vim 제공
  - 텍스트 입력, 수정 방식이 독특하여 처음엔 불편할 수 있음

### vi의 구조
- 
| 모드 | 설명 |
|------|------|
| 명령 모드(Command Mode) | 기본 모드 |
| 입력 모드(Insert Mode) | 글자 입력 |
| 마지막 모드(Last Mode) | 저장/종료 |

### 모드형과 비모드형 편집기
- 모드형 편집기(vi)
  - 입력 모드 : 텍스트를 입력
  - 명령 모드 : 텍스트 수정, 삭제, 복사, 붙여넣기 등 편집 기능 수행

- 비모드형 편집기(nano, emacs, 윈도 메모장, 워드프로세서)
  - 입력모드와 명령모드 구분 ❌
  - Ctrl, Alt 같은 특수 키 조합으로 편집 기능 수행
- 
| 구분 | 모드형(vi) | 비모드형(메모장) |
|------|-----------|------------------|
| 입력 모드 | 텍스트 입력 | 텍스트 입력 |
| 명령 모드의 예 | 복사하기 | yy | [Ctrl] + [C] |
| 명령 모드의 예 | 붙이기 | p | [Ctrl] + [V] |
| 명령 모드의 예 | 저장하기 | :w | [Ctrl] + [S] |
| 모드 전환 | i, a, o, [Esc] | 해당 없음 |

### vi 사용법과 환경 설정
- vi 시작
```
vi

- 기능 : 텍스트 파일을 편집
- 형식 : vi [파일]
- 사용 예 : vi // 빈 파일이 열리고 나중에 파일명을 정함
            vi test.txt // test.txt 파일이 열리고 이 파일이 없으면 빈 파일이 열림
```
- vi 종료
- 
| 명령어 | 의미 |
|--------|------|
| :w | 저장 |
| :q | 종료 |
| :wq | 저장 후 종료 |
| :q! | 강제 종료 |
- vi 모드 전환
- 
| 명령키  | 기능 |
|--------|-------|
| i | 현재 커서 자리에 입력 |
| a | 현재 커서의 다음 자리에 입력 |
| o | 커서가 위치한 행의 다음 행에 입력 |
| O | 커서가 위치한 행의 이전 행에 입력 |

#### vi 시작과 종료, 모드 전환하기 실습
```
1. 파일명을 지정하지 않고 vi를 실행한다
- vi
2. a를 입력하여 입력 모드로 전환한다
- a
3. 다음 내용을 입력한 뒤 Esc를 누른다
Happy those who do not follow
not sit in company with
4. 파일명을 vitest.txt로 지정하여 저장하고 종료한다
- :wq vitest.txt
```

### 커서와 화면 이동
- 커서 이동하기
  - vi에서는 마우스 사용 불가, 키보드로만 이동 가능
  - 방향키로도 커서 이동 가능 // 추천 ❌
- 
| 명령키 | 기능 |
|--------|------|
| h | 왼쪽 |
| l | 오른쪽 |
| j | 아래 |
| k | 위 |
- 화면 이동
- 
| 명령키 | 기능 |
|--------|------|
| G | 파일의 마지막 행으로 커서가 이동 |
| g | 파일의 첫 행으로 커서가 이동 |
| 행 번호G | 지정한 행번호로 커서 이동 |

### 내용 수정
- 
| 명령키 | 기능 |
|--------|------|
| r | 커서가 위치한 글자를 다른 글자로 수정 |
| cw, #cw | 커서 위치부터 단어의 끝까지 수정, #은 숫자 (예시) 3cw는 커서 위치부터 세 단어를 수정 |
| s, #s | 커서 위치부터 [Esc]를 입력할 때까지 수정, #은 숫자 (예시) 7s는 커서 위치부터 일곱 글자를 수정 |
- 한 글자 수정 : r 명령키
  - 커서를 수정할 글자 위에 놓고 r입력
  - 바꿀 글자를 입력하면 기존 글자가 변경됨
  - 명령 모드 유지(Esc 필요 없음)
- 여러 글자 수정 : cw 명령키
  - cw : 단어 단위 수정(커서 위치부터 단어 끝까지 변경)
  - 입력 후 [Esc]를 눌러 수정 완료

### 내용 삭제
- x : 커서 위치의 한 글자 삭제
- dd : 한 줄 삭제 // yy : 한 줄 복사
- 
| 명령키 | 기능 |
|--------|------|
| x, #x | 커서 위치의 글자를 삭제, #에는 삭제할 글자 수 지정 |
| dw, #dw | 커서 위치의 단어를 삭제, #에는 삭제할 단어 수를 지정 |
| dd, #dd | 커서 위치의 행을 삭제, #에는 삭제할 행의 수를 지정 |
| D | 커서 위치부터 행의 끝까지 삭제 |

### 명령 취소
- u : 가장 최근 실행한 명령 취소(한 단계 되돌리기)
- U : 현재 줄에서 수행한 모든 변경 취소
- Ctrl + Z는 취소가 아니라 vi를 일시 중단하고 셸로 나감
  프롬포트에서 fg 입력 시 vi로 복귀 가능 // jobs : fg 목록 보기
- 
| 명령키 | 기능 |
|--------|------|
| u | 직전에 수행한 명령을 취소 |
| U | 해당 행에서 실행한 모든 명령을 취소 |

#### vi로 입력, 수정, 삭제, 복구하기 실습
```
1. vi로 새로운 파일인 exec.txt파일을 연다
- vi exec.txt
2. i 명령키를 입력하여 입력 모드로 전환 후 다음 내용을 입력하고 파일을 저장한다
- i
Good morning everyone.
Nice to meet you.
I am a linux beginner.
Now introduce yourself.
3. 커서를 3행의 beginner의 b로 이동한다
- h(왼),j(아래),k(위),l(오른) 사용
4. beginner를 experts로 수정한다
- b커서 > cw > experts
5. 2행에 있는 meet의 첫 글자인 m으로 이동한다
- h(왼),j(아래),k(위),l(오른) 사용
6. meet를 단어 삭제 명령키로 삭제한다
- meet > dw
7. y부터 행의 끝까지 명령키 하나로 삭제한다
- y > D
8. 2행에 있는 meet의 첫 글자인 m으로 이동한다
- h(왼),j(아래),k(위),l(오른) 사용
9. 3행의 첫 글자로 이동하여 I am a를 We are로 수정한다
- I am a > xxxxxx > i > We are
10. 파일 내용을 저장하고 종료한다
- :wq
```

### 복사 및 잘라 붙이기
- yy : 현재 행 복사
- dd : 현재 행 잘라내기
- p : 복사/잘라낸 행을 현재 커서 아래에 붙이기
- 복사/잘라낸 내용은 임시 버퍼에 저장됨
- 붙이기 전에 다른 명령을 사용하면 버퍼 내용이 손실될 수 있음
- 
| 명령키 | 기능 |
|--------|------|
| yy, #yy | 커서가 위치한 행을 복사, #에는 복사할 행의 수를 지정 |
| p | 커서가 위치한 행의 아래쪽에 붙임 |
| P | 커서가 위치한 행의 위쪽에 붙임 |
| dd, #dd | 커서가 위치한 행을 잘라둠, #에는 잘라둘 행의 수를 지정 |
- 2yy : 2행 복사
- 5dd : 5행 잘라내기

### 검색 및 바꾸기
- 검색
- 
| 명령키 | 기능 |
|--------|------|
| /문자열 | 문자열 아래 방향으로 검색 |
| ?문자열 | 문자열 위 방향으로 검색 |
| n | 원래 찾던 방향으로 다음 문자열을 검색 |
| N | 역방향으로 다음 문자열을 검색 |
```
아래 방향으로 'Linux' 검색 : /Linux 입력 후 Enter
커서가 현재 위치 이후 첫 번째 'Linux'로 이동
```
- 바꾸기
- 
| 명령키 | 기능 |
|--------|------|
| :s/문자열1/문자열2 | 커서가 위치한 행에서 첫 번째로 나오는 문자열1을 문자열2로 바꿈 |
| :%s/문자열1/문자열2 | 파일 전체에서 모든 문자열1을 문자열2로 바꿈 |
```
현재 행에서 'Linux'를 'Hanbit'으로 변경
:s/Linux/Hanbit 입력
```
```
5~6행에서 'Hanbit'을 'Linux'로 변경
:5,6s/Hanbit/Linux/ 입력
```

### 파일 읽어오기
- :r 파일명 명령을 사용하면 지정한 파일의 내용이 현재 커서 위치에 삽입
```
:r exec.txt 명령을 실행하면 exec.txt의 내용이 test.txt의 4행 다음에 삽입
커서는 삽입된 내용의 첫 글자에 위치함
```

### 행 번호 표시하기
- 행 번호 표시 :set nu
- 행 번호 숨기기 :set nonu

### 특수 문자 표시하기
- 기본적으로 행 끝($)이나 탭(^I)같은 특수문자는 보이지 않음
- 특수문자 표시 :set list
- 특수문자 숨기기 :set nolist

#### 실습
- 1. 파일 생성
```
vi test.txt
```
- 2. 입력 모드
```
i
내용 입력:
Hello Linux
```
- 3. 저장
```
[Esc] > :w
```
- 4. 종료
```
:q
```
- 5. 한 줄 삭제
```
dd
```
- 6. 복사 & 붙여넣기  
```
yy
p
```

##### ✍️작성자: 박지안
##### 🐧실습 환경: VMware - Ubuntu
##### 🗓️ 작업일: 2026-04-12
