# 2학년 리눅스 1학기 기말 대비

## 7장
- 압축 사용법
```
z : gzip 압축 사용

압축  + gzip으로 백업할 경우
tar -cvzf backup_$today.tar.gz *.txt
```
- 압축 해제 사용법
```
x : 압축 해제(extract)
```
- 압축 풀기 사용법
```
단순히 묶인 파일만 풀기
tar -xvf backup_$today.tar
```
- find
  - 
  | 옵션 | 의미 |
  |------|------|
  | -name | 파일 이름 검색 |
  | -type f | 파일 |
  | -type d | 디렉토리 |
  | -size | 파일 크기 |
  - 특정 파일 찾기
    - find . -name "*.log"
  - 확장자 검색
    - find . -name "*.log"
- grep
  - 문자열 검색
    - grep "error" log.txt
  - 대소문자 무시
    - grep -i "error" log.txt
  - 폴더 전체 검색
    - grep -r "error"

## 8장
- diff : 두 파일 내용 비교
  - diff, diff3, cmp 파일의 내용을 비교할 때 사용할 수 있는 명령
  - 형식 : diff [옵션] 파일1 파일2
  - diff 명령을 옵션 없이 실행하면 두 파일 간의 차이점을 출력
  - -q 옵션 : 두 파일이 다름을 나타내는 간단한 메시지만 출력
  ```
  diff -q sample diff.dat
  ```
  - -c 옵션 : 파일의 최종 수정 시간과 함께 차이점 및 일부 동일한 내용을 출력 기본적으로 3행씩 출력되며, 다른 내용에는 ! 표시
- 프로세스의 개념
  - 프로세스는 현재 시스템에서 실행 중인 프로그램을 의미
  - 리눅스는 다중 프로세스 시스템으로, 여러 프로세스가 동시에 실행됨
  - 시스템 프로세스는 리눅스 시스템의 운영에 필요한 기능을 수행
  - 사용자 프로세스는 사용자 실행한 명령이나 프로그램을 처리
- 고아 프로세스
  - 자식 프로세스가 실행 중인데 부모 프로세스가 종료되면 발생
- 좀비 프로세스
  - 좀비 프로세스는 자식 프로세스가 종료했지만, 부모 프로세스가 종료 정보를 읽지 않아서 프로세스 목록에 남아 있는 상태
- ps : 프로세스 정보 검색 명령
  - 형식 : ps [옵션]
  - -e : 전체 프로세스 목록 출력하기
  - -f : 프로세스 상세 정보 출력하기
  - ps -e | more : | more을 사용하여 페이지 단위로 결과를 확인할 수 있음
- 특정 프로세스 정보 검색하기 : ps와 grep (객관식)
  - ps명령과 grep명령을 |(파이프)로 연결하여 특정 프로세스를 검색할 수 있음
  - ps -ef | grep 형태로 연결하여 시스템에서 실행 중인 프로세스 중 원하는 프로세스를 검색
- kill : 프로세스 종료
  - 형식 : kill [-시그널] PID
- top : 프로세스 관리 도구
  - top명령은 현재 실행 중인 프로세스의 정보를 주기적으로 출력하는 도구
  - %CPU : 퍼센트로 표시한 CPU 사용량
  - %MEN : 퍼센트로 표시한 메모리 사용량
  - TIME+ : CPU 누적 이용시간
- 포그라운드 작업
  - 사용자가 입력한 명령이 실행되어 결과를 화면에 출력할 때까지 기다리는 방식
- 백그라운드 작업
  - 명령이 실행될 때 처리가 끝나는 것과 관계없이 바로 프롬프트가 출력되어 사용자가 다른 작업을 계속할 수 있는 방식
  - 명령어의 끝에 & 기호 추가
- jobs : 작업 목록 보기
  - 형식 : jobs [%작업 번호]
- sleep : 지정한 시간동안 일시 정지시키는 명령어
- 작업 전환하기
  - 
  | 명령 | 기능 |
  |------|-----|
  | Ctrl + Z | 포그라운드 작업을 중지 |
  | bg %작업 번호 | 작업 번호가 지시하는 작업을 백그라운드 작업으로 전환 |
  | fg %작업 번호 | 작업 번호가 지시하는 작업을 포그라운드 작업으로 전환 |
  | Ctrl + C | 작업 종료 |
  - Ctrl + Z를 사용하여 작업을 일시 중지하고, 이후 bg %작업 번호 명령을 사용하여 백그라운드로 전환할 수 있음
  ```
  $ jobs → 현재 백그라운드 작업이 없음
  $ sleep 100 → 포그라운드 실행
  ^Z → Ctrl + Z로 일시 중지
  [1]+ 멈춤     sleep 100 → 일시 중지된 상태
  $ bg %1 → 백그라운드로 전환
  [1]+ sleep 100 &
  $ jobs
  [1]+ 실행중    sleep 100 & → 백그라운드 실행 중
  ```
9장:
23pg 시스템호출과 라이브러리의 차이점 이해하셔야해요 비교하시면 되고
make 파일 만드는건 안나오고 왜 사용하는지 목적 중요 [서술형]
-> 다중 소스 파일을 하나로 묶어서 사용하기 위해서
## 9장
- 컴파일과 인터프리터
  - 컴파일 : 고급 언어로 작성된 프로그램의 전체 코드를 한 번에 번역하여 기계어로 된 실행 파일을 생성
  - 인터프리터 : 실행 파일을 미리 생성하지 않고, 프로그램 실행 시 코드를 하나씩 번역하여 실행
  - 일반적으로 인터프리터 방식은 문법이 더 유연하고 배우기 쉬움
  - 셸 스크립트는 인터프리터 방식으로 실행됨
- gcc : gcc로 생성한 기본 실행 파일은 a.out
  ```
  gcc -o hello hello.c
  ./hello
  ```
  
